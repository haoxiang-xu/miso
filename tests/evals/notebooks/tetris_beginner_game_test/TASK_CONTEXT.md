# Task Context

The user wants a simple but polished browser-based Tetris game.

Default implementation target:

- static HTML/CSS/JavaScript
- game files under `app/`
- no backend

The user is a beginner.
If the model needs confirmation on a material decision, it must ask first instead of assuming.
