# Run Tests

Read the canonical guide at `docs/en/guides/test.md` and follow it step by step.

## Arguments
- $ARGUMENTS: Optional test filter pattern (e.g. "anthropic", "kernel", "memory", "toolkit")
