# Add a New Model

Read the canonical guide at `docs/en/guides/add-model.md` and follow it step by step.

## Arguments
- $ARGUMENTS: Model name and provider info (e.g. "claude-opus-4 anthropic" or "gpt-4.1-mini openai")
