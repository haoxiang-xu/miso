# Explore Architecture

Read the canonical guide at `docs/en/guides/explore.md` and follow it step by step.

## Arguments
- $ARGUMENTS: Area to explore (e.g. "kernel loop", "memory system", "tool execution", "provider abstraction", "subagents")
