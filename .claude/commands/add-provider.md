# Add a New LLM Provider

Read the canonical guide at `docs/en/guides/add-provider.md` and follow it step by step.

## Arguments
- $ARGUMENTS: Provider name and SDK package (e.g. "deepseek deepseek-sdk")
